# BrainTrace 상세 발표 스크립트 (10분 분량)

> **발표자 노트**: 이 스크립트는 BrainTrace의 기술적 깊이와 차별성을 강조하기 위해 작성되었습니다. `[슬라이드: ...]` 부분은 화면에 띄울 시각 자료를 의미합니다.

---

## 1. 서론: LLM의 한계와 BrainTrace의 탄생 (0:00 - 1:30)

**[슬라이드: "LLM의 두 가지 벽: 할루시네이션 & 최신성"]**

안녕하십니까. **BrainTrace** 프로젝트 발표를 시작하겠습니다.

우리는 바야흐로 대규모 언어 모델(LLM)의 시대에 살고 있습니다. 하지만 기업 현장에서 LLM을 도입하려 할 때, 우리는 여전히 두 가지 거대한 벽에 부딪힙니다.

첫째, **할루시네이션(Hallucination)**입니다. LLM은 모르는 것도 아는 척, 그럴듯하게 거짓말을 합니다. 기업의 의사결정에 치명적일 수 있죠.
둘째, **최신 정보의 부재**입니다. 학습 시점 이후의 데이터나, 기업 내부의 비공개 데이터는 LLM이 알지 못합니다.

**[슬라이드: 기존 RAG의 한계 (단순 검색의 문제)]**

이를 해결하기 위해 RAG(검색 증강 생성) 기술이 나왔지만, 기존 RAG는 단순히 문서를 잘라서(Chunking) 키워드로 검색하는 수준에 머물렀습니다. "문맥"이 끊기고, 파편화된 정보만으로는 복잡한 질문에 답할 수 없었죠.

**[슬라이드: BrainTrace 로고 및 핵심 컨셉 (Knowledge Graph + Agent)]**

그래서 우리는 **BrainTrace**를 개발했습니다.
BrainTrace는 단순한 검색기가 아닙니다. **지식 그래프(Knowledge Graph)**를 통해 데이터의 '관계'를 이해하고, **AI 에이전트(AI Agent)**를 통해 스스로 '생각'하고 검증하는 차세대 지능형 질의응답 시스템입니다.

---

## 2. 핵심 기술 1: 데이터가 지식이 되는 과정 (Data Ingestion) (1:30 - 4:00)

**[슬라이드: 데이터 적재 파이프라인 아키텍처]**

BrainTrace가 데이터를 어떻게 다루는지, 그 **적재 파이프라인**부터 살펴보겠습니다.

**[슬라이드: LLM 기반 지식 추출 (Node & Edge)]**

사용자가 PDF나 텍스트 파일을 업로드하면, 우리는 이를 단순히 자르지 않습니다.
고성능 LLM을 사용하여 텍스트를 읽고, 그 안에서 **중요한 개념(Node)**과 **개념 간의 관계(Edge)**를 추출합니다.
예를 들어, "애플이 아이폰을 출시했다"라는 문장이 있다면, `애플`과 `아이폰`이라는 노드를 만들고, `출시했다`라는 관계를 맺어주는 식이죠.

**[슬라이드: 소스 매핑 알고리즘 (Source Mapping Algorithm)]**

여기서 BrainTrace만의 독창적인 기술인 **'소스 매핑 알고리즘'**이 작동합니다.
추출된 지식 노드가 원본 문서의 **몇 페이지, 몇 번째 문장**에서 나왔는지를 역추적하여 매핑합니다.
이 과정은 벡터 유사도(Cosine Similarity)를 기반으로 정교하게 이루어집니다. 덕분에 나중에 AI가 답변을 할 때, "이 정보는 A보고서 5페이지 3번째 줄에 근거합니다"라고 **정확한 출처(Citation)**를 제시할 수 있게 됩니다.

**[슬라이드: 하이브리드 저장소 (Neo4j + Qdrant)]**

이렇게 구조화된 데이터는 두 곳의 저장소에 동시에 저장됩니다.
1.  **Neo4j (Graph DB)**: 데이터 간의 복잡한 연결 관계를 저장하여 '추론'을 가능하게 합니다.
2.  **Qdrant (Vector DB)**: 텍스트의 의미를 벡터로 저장하여, 키워드가 달라도 문맥으로 찾아내는 '의미 검색'을 담당합니다.

---

## 3. 핵심 기술 2: 에이전트 기반 검색 (Agentic Retrieval) (4:00 - 7:00)

**[슬라이드: 3단계 AI 에이전트 파이프라인]**

데이터가 준비되었으니, 이제 사용자가 질문을 던졌을 때 어떻게 답변하는지 보겠습니다.
BrainTrace는 단순히 DB를 검색해서 바로 답변하지 않습니다. **3단계의 AI 에이전트**가 마치 사람처럼 생각하며 답변을 만들어냅니다.

**[슬라이드: Step 1 - 노드 품질 평가 에이전트]**

첫 번째는 **'노드 품질 평가 에이전트'**입니다.
벡터 검색으로 가져온 수많은 노드 중에는 질문과 관련 없는 '노이즈'가 섞여 있을 수 있습니다. 이 에이전트는 "이 노드가 정말 질문에 필요한가?"를 판단하여 불필요한 정보를 과감히 버립니다. 이를 통해 답변의 정확도를 높입니다.

**[슬라이드: Step 2 - 스키마 충분성 판단 & 딥 서치]**

두 번째는 **'스키마 충분성 판단 에이전트'**입니다.
현재 가져온 정보만으로 답변이 가능한지 스스로 자문합니다. 만약 정보가 부족하다면?
여기서 **'딥 서치(Deep Search)'** 기능이 발동됩니다. 에이전트가 스스로 그래프를 더 깊이 탐색(Multi-hop)하여 숨겨진 연결 고리를 찾아냅니다.

**[슬라이드: Step 3 - 최적화 에이전트 & 자가 치유]**

세 번째는 **'최적화 에이전트'**입니다.
LLM에게 전달할 최종 프롬프트를 정제합니다. 불필요한 토큰을 줄여 비용을 절감하고, LLM이 핵심에만 집중하게 만듭니다.

또한, 이 모든 과정에서 에러가 발생하면 **'자가 치유(Self-Healing)'** 로직이 작동합니다. 네트워크 오류나 파싱 에러가 발생해도, 시스템이 스스로 재시도하거나 대체 경로를 찾아내어 사용자에게 어떻게든 답변을 전달합니다.

---

## 4. 확장 기능: 시뮬레이션 에이전트 (7:00 - 8:30)

**[슬라이드: 단순 질의를 넘어선 예측/시뮬레이션]**

BrainTrace는 과거의 정보를 묻는 것에 그치지 않습니다. 미래를 예측하는 **'시뮬레이션 에이전트'** 기능도 갖추고 있습니다.

**[슬라이드: 데모 시나리오 - "광고비를 줄이면?"]**

예를 들어, "광고비를 20% 줄이면 매출에 어떤 영향이 있을까?"라고 물으면, 단순 검색으로는 답을 알 수 없습니다.
이때는 **'Agent' 의도**로 분류되어, 별도의 파이썬 시뮬레이션 스크립트가 실행됩니다. 내부 로직에 따라 ROI 변화를 계산하고, 구체적인 수치와 함께 전략적인 조언을 제공합니다.

---

## 5. 결론 및 기대 효과 (8:30 - 10:00)

**[슬라이드: BrainTrace 도입 효과]**

정리하겠습니다. BrainTrace를 도입함으로써 얻을 수 있는 효과는 명확합니다.

1.  **정확성(Accuracy)**: 지식 그래프와 소스 매핑을 통해 할루시네이션을 최소화했습니다.
2.  **설명 가능성(Explainability)**: 답변의 근거를 명확히 제시하여 신뢰할 수 있습니다.
3.  **확장성(Extensibility)**: 단순 검색을 넘어, 예측과 분석까지 가능한 에이전트 시스템으로 확장했습니다.

**[슬라이드: Closing - "데이터를 살아있는 지식으로"]**

BrainTrace는 기업의 차가운 데이터 저장소에 지능을 불어넣어, 살아 숨 쉬는 지식 파트너로 만들어줄 것입니다.
이상으로 발표를 마치겠습니다. 경청해 주셔서 감사합니다.
