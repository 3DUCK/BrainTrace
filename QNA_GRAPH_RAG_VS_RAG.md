# BrainTrace 기술 심층 Q&A: Graph RAG vs. Traditional RAG 비교

이 문서는 **기존 RAG (Traditional RAG)**와 **Graph RAG**의 기술적 차이점, 그리고 각각의 장단점을 상세히 비교 분석합니다. BrainTrace가 왜 Graph RAG 방식을 채택했는지 이해하는 데 도움을 줍니다.

---

## 1. 핵심 차이점 (Key Differences)

| 특징 | Traditional RAG (기존 방식) | Graph RAG (BrainTrace 방식) |
| :--- | :--- | :--- |
| **데이터 구조** | **평면적 텍스트 청크 (Flat Chunks)**<br>문서를 단순히 일정 길이로 잘라서 저장 | **지식 그래프 (Knowledge Graph)**<br>개념(Node)과 관계(Edge)로 구조화하여 저장 |
| **검색 방식** | **벡터 유사도 검색 (Vector Search)**<br>질문과 키워드/의미가 비슷한 텍스트 조각을 찾음 | **관계 기반 탐색 (Graph Traversal)**<br>연결된 노드를 따라가며 숨겨진 맥락을 추적 |
| **문맥 이해** | **파편화됨 (Fragmented)**<br>잘린 텍스트 조각만 보므로 전체 맥락 파악이 어려움 | **연결됨 (Connected)**<br>A와 B가 어떻게 연결되는지 전체 구조를 이해함 |
| **답변 능력** | 단순 사실 조회 ("A가 뭐야?")에 강함 | 복합 추론 ("A가 B에 미친 영향은?")에 강함 |

---

## 2. Traditional RAG (기존 검색 증강 생성)

### ✅ 장점 (Pros)
1.  **구현이 쉽고 빠름**: 텍스트를 자르고 임베딩만 하면 되므로 구축 비용이 저렴합니다.
2.  **일반적인 질문에 효과적**: "애플의 CEO는 누구야?" 같은 단순 사실 확인 질문에는 매우 빠르고 정확합니다.
3.  **유연성**: 어떤 형태의 텍스트 데이터든 쉽게 넣을 수 있습니다.

### ❌ 단점 (Cons)
1.  **문맥 단절 (Context Loss)**: 문서를 자르는 과정에서 중요한 연결 고리가 끊길 수 있습니다.
2.  **종합적 추론 불가**: 여러 문서에 흩어진 정보를 조합해야 하는 질문("A문서와 B문서의 공통점은?")에 취약합니다.
3.  **낮은 설명력**: 왜 그런 답변이 나왔는지 논리적 근거를 제시하기 어렵습니다.

---

## 3. Graph RAG (지식 그래프 기반 RAG)

### ✅ 장점 (Pros)
1.  **깊이 있는 추론 (Deep Reasoning)**: "A가 B를 유발하고, B가 C에 영향을 줬으니, A는 C의 원인이다"와 같은 다단계 추론(Multi-hop Reasoning)이 가능합니다.
2.  **전체 맥락 파악 (Holistic View)**: 특정 키워드가 없어도, 관계를 통해 연관된 정보를 찾아낼 수 있습니다.
3.  **높은 설명 가능성 (XAI)**: "이 노드들이 이렇게 연결되어 있어서 이런 결론이 나왔습니다"라고 시각적/논리적 근거를 제시할 수 있습니다.
4.  **할루시네이션 감소**: 구조화된 지식(Fact)에 기반하므로 거짓 정보를 생성할 확률이 낮습니다.

### ❌ 단점 (Cons)
1.  **구축 비용 높음**: 데이터를 그래프로 변환(추출)하는 과정에 LLM 비용과 시간이 듭니다.
2.  **복잡성**: 그래프 DB(Neo4j 등)를 관리하고 쿼리하는 기술적 난이도가 높습니다.

---

## 4. 결론: BrainTrace의 하이브리드 전략

BrainTrace는 **Graph RAG의 강력한 추론 능력**과 **Vector RAG의 유연한 검색 능력**을 모두 활용하는 **하이브리드(Hybrid) 전략**을 취합니다.

*   **Qdrant (Vector)**: 사용자의 모호한 질문을 받아 검색의 **진입점**을 찾습니다.
*   **Neo4j (Graph)**: 진입점에서 시작하여 연결된 지식을 **확장하고 추론**합니다.

이로써 BrainTrace는 단순 검색을 넘어, **"생각하는 검색"**을 구현합니다.
