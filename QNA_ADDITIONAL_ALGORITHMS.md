# BrainTrace 기술 심층 Q&A: 추가 핵심 알고리즘 (의도 분석 & 딥 서치)

이 문서는 앞서 설명한 데이터 적재나 RAG 생성 로직 외에, BrainTrace의 지능을 담당하는 또 다른 중요한 알고리즘인 **의도 분석(Intent Analysis)**과 **딥 서치(Deep Search)** 로직에 대해 설명합니다.

---

## 1. 의도 분석 및 라우팅 알고리즘 (Intent Analysis & Routing)

### Q. 사용자의 질문이 어떤 유형인지 어떻게 아나요?
BrainTrace는 모든 질문을 무조건 검색(RAG)하지 않습니다. `IntentService`가 질문의 의도를 먼저 파악하여 가장 적절한 처리 경로로 안내합니다.

### 🔍 동작 원리
1.  **프롬프트 엔지니어링**: LLM에게 다음과 같은 분류 기준을 제시합니다.
    *   **SIMPLE**: "안녕", "너 누구야?" 같은 단순 인사나 일반 상식. -> *검색 없이 바로 LLM 대화*
    *   **RAG**: "A 문서 요약해줘", "B 개념이 뭐야?" 같은 지식 검색. -> *지식 그래프 검색*
    *   **AGENT**: "광고비 20% 줄이면 어떻게 돼?", "ROI 계산해줘" 같은 예측/계산. -> *외부 시뮬레이션 툴 실행*
2.  **파라미터 추출**: 만약 `AGENT` 의도라면, 질문 속에 숨어있는 변수(예: `광고비`, `20%`)를 JSON 형태로 추출해냅니다.
3.  **라우팅**: 분석 결과에 따라 `SIMPLE`, `RAG`, `AGENT` 중 하나의 파이프라인을 실행합니다.

### 💡 왜 중요한가요?
모든 질문에 대해 무겁게 그래프를 검색하면 응답 속도가 느려지고 비용이 낭비됩니다. 의도 분석을 통해 **필요한 경우에만** 고비용 연산을 수행하여 시스템 효율성을 극대화합니다.

---

## 2. 딥 서치 알고리즘 (Deep Search Algorithm)

### Q. '딥 서치'는 일반 검색과 무엇이 다른가요?
일반적인 검색(2-hop)은 직접 연결된 이웃 노드만 찾아보지만, **딥 서치**는 그래프를 타고 더 멀리, 더 깊게 탐색하여 숨겨진 연결 고리를 찾아냅니다.

### 🔍 동작 원리 (Neo4j Cypher Query)
`Neo4jHandler`의 `query_schema_by_node_names_deepSearch` 메서드에 구현되어 있습니다.

1.  **가변 경로 탐색 (Variable Length Path)**: `(start)-[*0..5]-(target)` 쿼리를 사용하여 최대 5단계 건너편에 있는 노드까지 탐색 범위를 확장합니다.
2.  **설명(Description) 기반 필터링**:
    *   무조건 멀리 가는 게 아니라, **'의미 있는 정보(Description)'가 있는 노드**를 만날 때까지 탐색합니다.
    *   중간에 설명이 없는 단순 연결 노드(Connective Node)는 건너뛰고(Skip), 정보가 담긴 노드를 찾으면 멈춥니다.
3.  **경로 수집**: 시작 노드에서 목표 노드까지 이어지는 전체 경로(Path)를 수집하여, 두 개념이 **어떻게 연결되어 있는지** 맥락을 파악합니다.

### 💡 언제 사용되나요?
*   사용자가 명시적으로 `use_deep_search: true` 옵션을 켰을 때.
*   **[AI Agent 2] 스키마 충분성 판단** 단계에서 "정보가 부족하다"고 판단했을 때, 시스템이 스스로 트리거합니다.

이 알고리즘 덕분에 BrainTrace는 단편적인 사실뿐만 아니라, **복잡한 인과관계나 흐름**을 설명할 수 있습니다.
