# BrainTrace 기술 심층 Q&A: KoE5 임베딩 모델

이 문서는 BrainTrace에서 텍스트의 의미를 벡터로 변환하는 데 사용하는 핵심 모델인 **KoE5**에 대해 설명합니다.

---

## Q. KoE5가 무엇인가요?

**KoE5**는 **"Korean E5"**의 약자로, 한국어 텍스트 임베딩(Embedding)에 특화된 고성능 AI 모델입니다.

*   **기반 모델**: Microsoft의 다국어 임베딩 모델인 `mE5` (Multilingual E5)를 기반으로 합니다.
*   **특징**: 한국어 데이터셋으로 추가 학습(Fine-tuning)되어, 한국어의 문맥과 뉘앙스를 이해하는 능력이 매우 뛰어납니다.
*   **역할**: 사람이 읽는 '글자(Text)'를 컴퓨터가 계산할 수 있는 '숫자 배열(Vector)'로 변환해줍니다.

---

## Q. BrainTrace에서 KoE5는 어디에 사용되나요?

BrainTrace의 `backend/services/embedding_service.py` 파일에서 핵심적으로 사용됩니다.

### 1. 지식 그래프 노드 벡터화 (Data Ingestion)
*   **과정**: LLM이 추출한 지식 그래프의 **노드 이름**과 **설명(Description)**을 KoE5 모델에 통과시킵니다.
*   **결과**: 각 노드는 768차원(또는 1024차원)의 고유한 벡터값을 가지게 되며, 이 값은 **Qdrant(벡터 DB)**에 저장됩니다.

### 2. 사용자 질문 벡터화 (RAG Generation)
*   **과정**: 사용자가 질문을 입력하면(예: "애플의 신제품은?"), 이 질문 역시 KoE5를 통해 실시간으로 벡터화됩니다.
*   **결과**: 질문 벡터와 가장 유사한(거리가 가까운) 노드 벡터를 Qdrant에서 찾아냄으로써, **의미 기반 검색(Semantic Search)**을 수행합니다.

### 3. 소스 매핑 (Source Mapping)
*   **과정**: 원본 문서의 문장들과 추출된 키워드 간의 유사도를 계산할 때도 KoE5가 생성한 벡터를 사용합니다.
*   **결과**: 단순 단어 매칭이 아니라, **의미가 통하는 문장**을 정확히 찾아내어 출처로 연결할 수 있습니다.

---

## Q. 왜 KoE5를 선택했나요?

1.  **한국어 성능**: 일반적인 다국어 모델(OpenAI 등)보다 한국어 특화 데이터로 학습되어 국내 기업 문서 처리에 더 적합합니다.
2.  **오픈소스 & 로컬 구동**: 외부 API를 호출하지 않고 **서버 내부에 직접 설치(On-premise)**하여 돌릴 수 있어, **데이터 보안**이 중요한 기업 환경에 최적화되어 있습니다.
