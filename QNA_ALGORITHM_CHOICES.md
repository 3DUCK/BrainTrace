# BrainTrace 기술 심층 Q&A: 알고리즘 선정 이유

이 문서는 BrainTrace 개발 과정에서 특정 알고리즘이나 수식을 선택한 기술적 배경과 이유를 설명합니다.

---

## Q. 소스 매핑에서 왜 '유클리드 거리'가 아닌 '코사인 유사도(Cosine Similarity)'를 사용하나요?

BrainTrace의 소스 매핑 알고리즘은 추출된 **노드(키워드)**와 원본 **문장**을 연결할 때 **코사인 유사도**를 사용합니다. 유클리드 거리(Euclidean Distance)나 맨해튼 거리 등 다른 지표 대신 코사인 유사도를 선택한 이유는 다음과 같습니다.

### 1. 벡터의 '방향(의미)'에 집중하기 위해
*   **개념**: 임베딩 벡터 공간에서 **벡터의 길이(Magnitude)**는 텍스트의 물리적 길이(단어 수)나 빈도에 영향을 많이 받습니다. 반면, **벡터의 방향(Direction)**은 텍스트가 담고 있는 **'의미(Semantics)'**를 나타냅니다.
*   **이유**: 소스 매핑은 **"짧은 단어(노드 이름)"**와 **"긴 문장(원본)"**을 비교하는 작업입니다. 물리적 길이가 달라도 의미가 같다면 매칭되어야 합니다.
    *   *유클리드 거리*: 길이가 다르면 거리가 멀다고 판단할 수 있습니다. (부적합)
    *   *코사인 유사도*: 길이와 상관없이 두 벡터가 가리키는 방향(각도)이 비슷하면 유사하다고 판단합니다. (적합)

### 2. 고차원 공간에서의 강건함 (Robustness in High Dimensions)
*   **개념**: 텍스트 임베딩(KoE5 등)은 보통 768차원 이상의 고차원 데이터를 다룹니다.
*   **이유**: 차원이 높아질수록 유클리드 거리는 모든 점들 사이의 거리가 멀어지고 비슷해지는 **'차원의 저주(Curse of Dimensionality)'** 현상이 발생하기 쉽습니다. 반면 코사인 유사도는 고차원에서도 비교적 안정적인 성능을 보여줍니다.

### 3. 임베딩 모델의 학습 목적과 일치
*   **이유**: BrainTrace가 사용하는 `KoE5`나 `OpenAI text-embedding-3` 등 대부분의 최신 임베딩 모델들은 학습 단계에서부터 **코사인 유사도(또는 내적)를 최대화하는 방향**으로 학습됩니다. 즉, 모델 자체가 "코사인 유사도로 쟀을 때 가장 정확하도록" 만들어졌기 때문에, 이를 따르는 것이 가장 성능이 좋습니다.

---

## 요약
> **"길이(양)가 아니라 의미(질)를 비교해야 하기 때문입니다."**
>
> 짧은 키워드(Node)와 긴 문장(Sentence) 사이의 의미적 연관성을 찾기에는, 벡터의 크기를 무시하고 **방향(의미)**만을 비교하는 **코사인 유사도**가 수학적으로 가장 적합합니다.
