# BrainTrace 기술 심층 Q&A: 데이터 적재 및 Graph RAG 저장 로직

이 문서는 BrainTrace 프로젝트의 **데이터 적재(Data Ingestion)** 과정에 대한 상세한 기술적 설명을 담고 있습니다. 비정형 문서가 어떻게 구조화된 지식 그래프로 변환되어 저장되는지, 그 내부 로직을 단계별로 설명합니다.

---

## Q. 문서가 Graph RAG에 저장되는 전체 흐름은 어떻게 되나요?

문서가 업로드되면 시스템은 다음 **5단계 파이프라인**을 거쳐 데이터를 처리합니다. 단순한 텍스트 저장이 아니라, LLM을 활용해 지식 구조를 추출하고 매핑하는 고도화된 과정입니다.

### 1. 텍스트 청킹 (Text Chunking)
*   **목적**: LLM의 컨텍스트 윈도우 제한을 극복하고, 정보 처리를 효율화하기 위함입니다.
*   **로직**: 문서를 단순히 페이지나 글자 수로 자르지 않고, **Overlap(중첩)**을 적용하여 자릅니다. 이는 문맥이 끊기는 것을 방지하여, 문장과 문장 사이의 연결 고리를 유지하는 데 필수적입니다.

### 2. LLM 기반 그래프 요소 추출 (Graph Extraction)
*   **목적**: 비정형 텍스트에서 정형화된 지식(Node, Edge)을 뽑아냅니다.
*   **로직**: 청킹된 텍스트를 LLM(GPT-4o 등)에게 전달하여 다음과 같은 JSON 구조로 추출을 요청합니다.
    *   **Node (개념)**: `이름(Name)`, `타입(Label)`, `설명(Description)`
    *   **Edge (관계)**: `출발 노드` -> `관계 유형` -> `도착 노드`
    *   *예시*: "애플이 2024년에 아이폰16을 출시했다" -> `(애플)-[출시했다]->(아이폰16)`

### 3. 소스 매핑 알고리즘 (Source Mapping Algorithm)
*   **목적**: 답변의 **정확한 근거(Citation)**를 제시하기 위해, 추출된 지식이 원본의 어디에서 왔는지 추적합니다.
*   **로직**:
    1.  원본 텍스트를 **문장 단위(Sentence Splitting)**로 세분화합니다.
    2.  추출된 **노드 이름**과 **각 문장**을 임베딩 모델(KoE5)로 벡터화합니다.
    3.  **코사인 유사도(Cosine Similarity)**를 계산하여, 유사도가 높은 문장들을 해당 노드의 `original_sentences` 속성으로 매핑합니다.
    *   *결과*: 나중에 사용자가 질문했을 때, "이 정보는 5페이지 3번째 줄에 근거합니다"라고 명확히 답변할 수 있게 됩니다.

### 4. 그래프 데이터베이스 저장 (Neo4j Storage)
*   **목적**: 추출된 지식 네트워크를 영구 저장하고, 관계 기반 추론을 가능하게 합니다.
*   **로직**: **MERGE (Upsert)** 전략을 사용합니다.
    *   **기존 노드 존재 시**: 새로운 설명(Description)과 출처 문장(Source Sentence)을 리스트에 **추가(Append)**합니다. 정보가 누적되면서 지식이 풍부해집니다.
    *   **신규 노드**: 새로운 노드와 관계를 생성합니다.
    *   이 과정을 통해 파편화된 문서들이 하나의 거대한 **지식 그래프**로 연결됩니다.

### 5. 벡터 인덱싱 (Vector Indexing - Qdrant)
*   **목적**: 사용자의 모호한 질문에도 의미 기반 검색(Semantic Search)이 가능하도록 합니다.
*   **로직**:
    *   노드의 `이름`과 `설명`을 합쳐서 텍스트로 만듭니다. (예: "아이폰16 애플이 2024년 출시한 스마트폰")
    *   이를 **KoE5(한국어 특화 임베딩 모델)**을 사용해 고차원 벡터로 변환합니다.
    *   벡터 DB인 **Qdrant**에 저장하여, 키워드가 정확히 일치하지 않아도 문맥으로 해당 노드를 찾을 수 있게 합니다.

---

## Q. 왜 Neo4j와 Qdrant 두 가지 DB를 쓰나요?

BrainTrace는 **하이브리드 검색(Hybrid Retrieval)**을 위해 두 저장소를 모두 사용합니다.

1.  **Neo4j (Graph DB)**:
    *   **역할**: 데이터 간의 **'관계'**를 저장합니다.
    *   **용도**: "A와 B는 어떤 관계야?", "A의 영향으로 발생한 결과는?" 같은 **추론형 질문**에 답하기 위해 그래프를 탐색(Multi-hop)할 때 사용됩니다.

2.  **Qdrant (Vector DB)**:
    *   **역할**: 데이터의 **'의미'**를 저장합니다.
    *   **용도**: 사용자가 정확한 용어를 모르고 "그 작년에 나온 스마트폰 있잖아"라고 물었을 때, 의미적으로 유사한 `아이폰16` 노드를 찾아내는 **검색 진입점** 역할을 합니다.

이 두 기술의 결합이 바로 BrainTrace가 기존 RAG보다 뛰어난 답변 품질을 보여주는 핵심 이유입니다.
