# BrainTrace 기술 심층 Q&A: RAG 기반 답변 생성 로직

이 문서는 BrainTrace 프로젝트의 **RAG(검색 증강 생성) 기반 답변 생성** 과정에 대한 상세한 기술적 설명을 담고 있습니다. 사용자의 질문이 시스템에 들어왔을 때, 어떻게 관련 자료를 찾고(Retrieval), AI 에이전트들이 어떻게 협업하여 최종 답변을 생성하는지(Generation) 그 내부 로직을 단계별로 설명합니다.

---

## Q. 사용자가 질문하면 답변은 어떤 과정을 거쳐 만들어지나요?

BrainTrace의 답변 생성 과정은 단순한 검색이 아니라, **3단계 AI 에이전트 파이프라인**이 개입하는 지능형 프로세스입니다. 전체 흐름은 다음과 같습니다.

### 1. 초기화 및 벡터 검색 (Vector Search)
가장 먼저 질문과 관련된 지식 그래프의 **진입점(노드)**을 찾습니다.

*   **질문 임베딩**: 사용자의 질문을 KoE5 모델을 사용해 벡터로 변환합니다.
*   **유사 노드 검색**: 질문 벡터와 가장 유사한 노드들을 Qdrant(벡터 DB)에서 검색합니다.
    *   *예외 처리*: 만약 검색된 노드가 하나도 없다면, 즉시 **일반 지식(General Knowledge)** 모드로 전환하여 LLM이 배경지식으로 답변하고 종료합니다.

### 2. [AI Agent 1] 노드 품질 평가 (Node Quality Evaluation)
검색된 노드들이 진짜 질문과 관련이 있는지 AI가 다시 한번 검증합니다.

*   **역할**: 벡터 유사도는 높지만 실제 의미상 관련 없는 노드(Noise)를 제거합니다.
*   **동작**:
    *   LLM에게 "이 질문에 대해 이 노드들이 정말 필요한가?"라고 물어봅니다.
    *   불필요한 노드는 리스트에서 제외(Filtering)합니다.
*   **오류 복구(Self-Healing)**: 평가 과정에서 에러가 발생하면, **오류 복구 에이전트**가 개입하여 재시도하거나, 정 안되면 원본 노드 리스트를 그대로 사용하는 `skip` 전략을 선택합니다.

### 3. 스키마 조회 (Schema Retrieval)
검증된 노드들을 중심으로 연결된 지식(관계)을 그래프 DB에서 가져옵니다.

*   **기본 조회**: 선택된 노드들과 직접 연결된 **2-hop(두 단계 건너)** 범위의 노드 및 관계를 Neo4j에서 조회합니다.
*   **딥 서치(Deep Search)**: 만약 사용자가 `use_deep_search` 옵션을 켰다면, 더 깊거나 넓은 범위의 탐색을 시도합니다.
*   *예외 처리*: 조회 결과가 비어있다면, 일반 지식 답변으로 전환합니다.

### 4. [AI Agent 2] 스키마 충분성 판단 (Schema Sufficiency Check)
가져온 지식(스키마)만으로 답변이 가능한지 AI가 판단합니다.

*   **역할**: "현재 가져온 정보로 질문에 완벽히 답할 수 있는가?"를 평가합니다.
*   **동작**:
    *   정보가 부족하다고 판단되면(`is_sufficient: false`), 스스로 **딥 서치(Deep Search)**를 트리거하여 추가 정보를 가져옵니다.
    *   이미 충분하다면 다음 단계로 넘어갑니다.

### 5. 스키마 텍스트 생성 및 [AI Agent 3] 최적화
그래프 데이터를 LLM이 읽을 수 있는 텍스트로 변환하고, 길이를 최적화합니다.

*   **텍스트 변환**: 노드 정보와 관계(Edge) 정보를 `Node A -> Relation -> Node B` 형태의 자연어 텍스트로 변환합니다.
*   **[AI Agent 3] 텍스트 최적화**:
    *   변환된 텍스트가 너무 길거나 불필요한 정보가 많을 수 있습니다.
    *   AI가 질문에 답변하는 데 **꼭 필요한 정보만 남기고 요약/삭제**하여 프롬프트 비용을 절약하고 정확도를 높입니다.

### 6. 최종 답변 생성 (LLM Generation)
최적화된 지식(Context)과 질문을 합쳐 최종 답변을 생성합니다.

*   **생성**: "다음 지식 그래프 컨텍스트를 바탕으로 답변해줘"라는 프롬프트로 LLM(GPT/Ollama)을 호출합니다.
*   **할루시네이션 방지**: 컨텍스트에 없는 내용은 "지식그래프에 해당 정보가 없습니다"라고 답변하도록 지시되어 있습니다.
*   *재시도 로직*: 답변 생성에 실패하면 파라미터를 수정하거나 재시도하는 복구 로직이 동작합니다.

### 7. 후처리 및 검증 (Post-Processing & Verification)
답변이 생성된 후, 근거 자료를 정리하고 정확도를 계산합니다.

*   **참조 노드 추출**:
    *   답변 텍스트를 다시 임베딩하여, 지식 그래프의 노드들과 유사도를 비교합니다.
    *   실제 답변에 사용된 것으로 추정되는 **출처 노드(Referenced Nodes)**를 역추적합니다.
*   **정확도(Accuracy) 산출**:
    *   다음 3가지 지표의 가중합으로 점수를 매깁니다.
        1.  **Q (Retrieval Quality)**: 검색 품질
        2.  **S (Semantic Similarity)**: 답변과 근거 문서 간의 의미적 유사도
        3.  **C (Coverage)**: 제공된 정보 대비 답변이 커버한 비율
*   **출처 메타데이터 매핑**:
    *   참조된 노드가 어떤 파일(PDF, TXT 등)에서 왔는지 SQLite와 Neo4j를 조회하여 **원본 문장(Original Sentences)**과 **파일 제목**을 찾아 답변에 첨부합니다.

### 8. 저장 및 반환
*   최종적으로 `[RAG]` 태그가 붙은 답변, 참조된 노드 목록, 정확도 점수, 원본 파일 출처 등을 묶어 클라이언트에게 반환하고, 채팅 이력을 DB에 저장합니다.

---

## Q. 에이전트가 개입하면 무엇이 좋아지나요?

단순 검색 방식(Traditional RAG)과 비교했을 때, 3단계 에이전트 파이프라인은 다음과 같은 이점을 제공합니다.

1.  **정확도 향상**: 노이즈를 제거하고(Agent 1), 부족한 정보를 스스로 보충(Agent 2)하므로 답변의 품질이 획기적으로 높아집니다.
2.  **비용 절감**: 불필요한 정보를 최적화(Agent 3)하여 LLM에 전달하므로 토큰 사용량을 줄일 수 있습니다.
3.  **안정성**: 각 단계에서 오류가 발생해도 **자가 치유(Self-Healing)** 로직이 동작하여 시스템이 멈추지 않고 답변을 생성합니다.
