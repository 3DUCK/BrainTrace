# BrainTrace 프로젝트 3분 피칭 스크립트

## [0:00-0:30] 오프닝: 왜 BrainTrace인가?

안녕하십니까. **BrainTrace** 프로젝트를 소개하겠습니다.

오늘날 많은 기업들이 생성형 AI 도입을 원하지만, **'데이터 보안'**이라는 큰 장벽에 막혀 있습니다. 클라우드 LLM에 사내 기밀 자료를 업로드했다가 유출될까 두려워 온전히 활용하지 못하는 것이 현실입니다.

**BrainTrace**는 이러한 기업의 고민을 해결하는 **온프레미스 기반의 Graph RAG 솔루션**입니다. 외부 네트워크 연결 없이 **로컬 LLM**만으로 구동되어, 보안 걱정 없이 안전하게 원하는 답변을 얻을 수 있습니다.

핵심은 **Graph RAG** 기술입니다. 회사의 방대한 문서들을 단순 텍스트가 아닌 **'노드'와 '관계'**로 구조화하여 저장합니다. 덕분에 AI는 사내 지식의 맥락을 정확히 이해하고, 철저히 **저장된 자료에 기반한 신뢰할 수 있는 답변**을 제공합니다.

---

## [0:30-1:30] 핵심 아키텍처: 데이터가 지식이 되기까지

BrainTrace의 핵심은 크게 **데이터 적재**와 **지능형 검색**, 두 가지 파이프라인으로 나뉩니다.

먼저, **데이터 적재 과정**입니다.
사용자가 문서를 업로드하면, 시스템은 텍스트를 단순히 자르는 것이 아니라, LLM을 통해 **중요한 개념(Node)과 관계(Edge)**를 추출합니다.
여기서 독창적인 점은 **'소스 매핑 알고리즘'**입니다. 추출된 지식 노드가 원본 문서의 몇 번째 줄에서 나왔는지 역추적하여 매핑합니다. 덕분에 나중에 답변할 때 "이 정보는 A문서 5페이지에서 왔습니다"라고 정확한 근거를 제시할 수 있습니다. 이렇게 구조화된 데이터는 **Neo4j(그래프 DB)**와 **Qdrant(벡터 DB)**에 동시에 저장되어, 의미 검색과 관계 탐색이 모두 가능해집니다.

---

## [1:30-2:30] 차별점: 스스로 판단하는 AI 에이전트

다음은 BrainTrace만의 가장 강력한 무기, **'에이전트 기반 검색(Agentic Retrieval)'**입니다.

사용자가 질문을 던지면, 단순히 DB를 뒤져서 바로 답변하지 않습니다. **3단계의 AI 에이전트**가 개입하여 답변 품질을 극대화합니다.

1.  **첫째, 노드 품질 평가 에이전트**가 검색된 결과 중 불필요한 노이즈를 걸러냅니다.
2.  **둘째, 스키마 충분성 판단 에이전트**가 "이 정보만으로 답변이 가능한가?"를 스스로 자문합니다. 만약 부족하다면, 자동으로 **'딥 서치(Deep Search)'**를 수행하여 더 깊은 관계를 탐색합니다.
3.  **셋째, 최적화 에이전트**가 LLM에게 전달할 최종 프롬프트를 정제하여, 비용은 줄이고 정확도는 높입니다.

심지어 이 과정에서 에러가 발생하면, **'자가 치유(Self-Healing)'** 로직이 동작하여 스스로 복구를 시도합니다.

---

## [2:30-3:00] 클로징: 임팩트 및 비전

결과적으로 BrainTrace는 기존 RAG 대비 **답변의 정확도를 획기적으로 높였으며**, 무엇보다 **'설명 가능한 AI(XAI)'**를 구현했습니다.

단순히 "답이 이렇습니다"가 아니라, "이 지식 노드들이 연결되어 이런 결론이 나왔습니다"라고 시각적으로 보여줄 수 있습니다. BrainTrace는 기업의 방대한 비정형 데이터를 살아있는 지식으로 바꾸는 가장 확실한 솔루션이 될 것입니다.

감사합니다.
