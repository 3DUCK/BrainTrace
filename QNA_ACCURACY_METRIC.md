# BrainTrace 기술 심층 Q&A: 답변 정확도(Accuracy) 산출 로직

이 문서는 BrainTrace가 생성된 답변의 품질을 어떻게 정량적으로 평가하는지, 그 **정확도(Accuracy) 산출 알고리즘**에 대한 상세한 설명을 담고 있습니다.

---

## Q. 답변의 정확도는 어떻게 계산되나요?

BrainTrace는 단순히 하나의 지표에 의존하지 않고, **3가지 핵심 지표의 가중합(Weighted Sum)**을 통해 종합적인 정확도를 산출합니다. 결과값은 0.0에서 1.0 사이의 값으로 도출되며, 이를 백분율(%)로 환산하여 표시할 수 있습니다.

### 📊 산출 공식

$$Accuracy = (w_Q \times Q) + (w_S \times S) + (w_C \times C)$$

*   **Q (Retrieval Quality)**: 검색 품질
*   **S (Semantic Similarity)**: 의미적 유사도
*   **C (Coverage)**: 정보 커버리지
*   **w**: 각 지표의 가중치 (기본값: $w_Q=0.2, w_S=0.7, w_C=0.1$)

---

## Q. 각 지표(Q, S, C)는 무엇을 의미하나요?

### 1. Q (Retrieval Quality, 검색 품질)
*   **정의**: 사용자의 질문과 검색된 지식 노드들이 **벡터 공간에서 얼마나 가까운가**를 나타냅니다.
*   **계산 방식**: Qdrant(벡터 DB)에서 반환된 상위 노드들의 **유사도 점수(Cosine Similarity Score)의 평균값**입니다.
*   **의미**: "질문과 관련된 자료를 얼마나 잘 찾아왔는가?"를 평가합니다.

### 2. S (Semantic Similarity, 의미적 유사도)
*   **정의**: 생성된 **최종 답변**과 답변에 사용된 **근거 자료(노드 설명)**가 의미적으로 얼마나 일치하는지를 나타냅니다. **가중치가 0.7로 가장 높은 핵심 지표**입니다.
*   **계산 방식**:
    1.  LLM이 생성한 답변 텍스트를 벡터화합니다.
    2.  답변이 참고했다고 명시한 노드들의 설명(Description)을 모아 벡터화합니다.
    3.  두 벡터 간의 **코사인 유사도**를 계산합니다.
*   **의미**: "가져온 자료를 바탕으로 올바르게 답변했는가? (할루시네이션 여부)"를 평가합니다.

### 3. C (Coverage, 커버리지)
*   **정의**: 시스템이 제공한 전체 지식(Context) 중에서 답변이 **얼마나 많은 부분을 활용했는지**를 나타냅니다.
*   **계산 방식**:
    $$C = \frac{\text{답변이 실제 참고한 노드 수}}{\text{프롬프트로 제공된 전체 노드 수}}$$
*   **의미**: "제공된 정보를 누락 없이 충분히 활용했는가?"를 평가합니다.

---

## Q. 왜 이런 복합 지표를 사용하나요?

단순히 검색 유사도(Q)만으로는 LLM이 엉뚱한 답변을 하는 것을 잡아낼 수 없고, 의미 유사도(S)만으로는 정보가 누락된 것을 알기 어렵습니다.

BrainTrace는 **검색(Q) -> 생성(S) -> 활용(C)**의 전 과정을 아우르는 복합 지표를 사용하여, 사용자가 답변의 신뢰도를 직관적으로 판단할 수 있도록 돕습니다. 특히 **S 지표에 높은 가중치**를 두어, 근거 없는 거짓 답변(Hallucination)에 엄격하게 페널티를 부여하도록 설계되었습니다.
