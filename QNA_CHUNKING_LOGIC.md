# BrainTrace 기술 심층 Q&A: 텍스트 청킹(Chunking) 로직

이 문서는 BrainTrace가 긴 문서를 처리할 때 사용하는 **청킹(Chunking) 알고리즘**의 동작 원리와 그 필요성에 대해 상세히 설명합니다. 특히 BrainTrace는 단순한 길이 기반 자르기가 아닌, **재귀적(Recursive)이고 의미 기반(Semantic)**의 청킹 방식을 사용합니다.

---

## Q. 왜 텍스트를 청킹(Chunking)해야 하나요?

긴 문서를 통째로 처리하지 않고 작게 나누는 이유는 크게 3가지입니다.

1.  **LLM의 컨텍스트 한계 극복**: 모든 LLM은 한 번에 읽을 수 있는 글자 수(Token Limit)에 제한이 있습니다. 긴 논문이나 보고서를 한 번에 넣으면 에러가 나거나 앞부분 내용을 까먹습니다.
2.  **검색 정확도 향상**: 사용자의 질문은 보통 구체적입니다. 문서 전체를 검색 대상으로 하면 정보가 희석되지만, 작게 나누어 놓으면 질문과 정확히 일치하는 **'핵심 조각'**을 찾기 쉽습니다.
3.  **의미적 응집성 유지**: 문서를 주제별로 나누면, 각 청크가 하나의 완결된 의미를 가지게 되어 AI가 내용을 이해하고 요약하기 훨씬 수월해집니다.

---

## Q. BrainTrace의 청킹 함수는 어떻게 동작하나요?

BrainTrace는 `manual_chunking_sentences.py`에 구현된 **재귀적 청킹(Recursive Chunking)** 알고리즘을 사용합니다. 이는 단순히 글자 수로 자르는 것이 아니라, **내용의 유사도**를 보고 자를 위치를 결정합니다.

### 1. 문장 분리 및 토큰화
먼저 문서를 **문장(Sentence)** 단위로 쪼개고, 각 문장을 형태소 분석기(Okt)를 통해 명사 위주로 **토큰화**합니다.

### 2. 재귀적 분할 (Recursive Splitting)
전체 텍스트에서 시작하여 다음 과정을 반복하며 점점 작게 쪼갭니다.

*   **유사도 계산**: 인접한 문장들끼리 내용이 얼마나 비슷한지(유사도)를 계산합니다.
*   **임계값(Threshold) 적용**: 유사도가 급격히 낮아지는 지점, 즉 **주제가 바뀌는 지점**을 찾아 절단합니다.
*   **종료 조건**:
    *   청크 크기가 너무 작아지거나 (예: 20토큰 이하)
    *   깊이(Depth)가 너무 깊어지면 (예: 5단계 이상)
    *   분할을 멈추고 해당 청크를 확정합니다.

### 3. 키워드 추출 (Keyword Extraction)
각 청크가 무엇에 관한 내용인지 알기 위해 **LDA(잠재 디리클레 할당)**와 **TF-IDF** 알고리즘을 사용하여 핵심 키워드를 뽑아냅니다. 이 키워드는 나중에 지식 그래프의 **노드 이름**이 됩니다.

---

## Q. 이 함수는 언제 사용되나요?

주로 **데이터 적재(Data Ingestion)** 단계인 `POST /brainGraph/process_text` API에서 사용됩니다.

1.  **긴 텍스트 처리**: 업로드된 텍스트가 2000자 이상으로 길 때, 이 함수가 자동으로 발동하여 문서를 계층적으로 구조화합니다.
2.  **LLM 미사용 모드**: 사용자가 GPT나 Ollama 같은 외부 LLM을 사용하지 않도록 설정했을 때, 이 내부 알고리즘이 LLM을 대신하여 문서를 분석하고 그래프를 생성합니다.

결론적으로, 이 청킹 로직은 **외부 AI 의존 없이도** 긴 문서를 논리적으로 구조화하여 지식 그래프로 만들 수 있게 해주는 BrainTrace의 핵심 엔진입니다.
